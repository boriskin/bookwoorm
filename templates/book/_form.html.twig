<div class="container">
    <div class="row mx-auto">
        <div class="col-md-2"></div>
        <div class="col-md-8">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-bookwoorm">{{ button_label|default('Сохранить') }}</button>
            {{ form_end(form) }}
        </div>
        <div class="col-md-2"></div>
    </div>
</div>
